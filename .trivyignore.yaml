# Trivy suppressions.
#
# Every entry must be scoped to a path, carry a justification, and set an
# expiry. An entry without an expiry is an alarm that has been switched off
# permanently, which is worse than no alarm at all.
#
# Review process: when an entry expires, CI goes red again and we re-decide.
# Do not extend an expiry without re-reading the advisory.
#
# There are currently no suppressions. CVE-2026-56854 (golang.org/x/crypto/ssh)
# was suppressed here while no OPA release shipped the fix; OPA 1.20.2 ships
# golang.org/x/crypto 0.55.0, so the entry was deleted rather than extended.
# Keep it that way: prefer upgrading the base image over adding an entry below.

vulnerabilities: []
